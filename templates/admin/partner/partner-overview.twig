{% extends 'admin/base.html.twig' %}

{% block title %}{% trans %}Partners{% endtrans %}{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row header-row">
            <div class="col-6">
                <h1>{% trans %}Partners{% endtrans %}</h1>
            </div>
            <div class="col-6">
                <a href="{{ getLocalizedRoute('admin_partner_create', {locale: 'en'}) }}" class="btn btn-dark float-right">{% trans %}New EN{% endtrans %}</a>
                <a href="{{ getLocalizedRoute('admin_partner_create', {locale: 'nl'}) }}" class="btn btn-dark float-right">{% trans %}New NL{% endtrans %}</a>
            </div>
        </div>

        <table class="table table-striped table-hover">
            <thead class="thead-inverse">
            <tr>
                <th>ID</th>
                <th>{% trans %}Language{% endtrans %}</th>
                <th>{% trans %}Name{% endtrans %}</th>
            </tr>
            </thead>
            <tbody>
            {% for partner in partners %}
                <tr>
                    <td>{{ partner.id }}</td>
                    <td>
                        {% if partner.locale == 'nl' %}
                            <img src="{{ asset('img/dutch.png') }}" class="language-flag">
                        {% elseif partner.locale == 'en' %}
                            <img src="{{ asset('img/english.png') }}" class="language-flag">
                        {% else %}
                            {{ partner.locale|upper }}
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ getLocalizedRoute('admin_partner_edit', {'id': partner.id, 'locale': partner.locale}) }}">
                            {{ partner.name }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
